#! /usr/bin/env zsh

# I adapted this from code I didn't save the link for

color=0;
col=4;

for color in {0..255}; do
    fg_color=$((255 - $color))
    print -nP "$color: %F{$color}hello%f %K{$color}world%k "
    if ((color % col == (col-1))); then
        echo "";
    fi
done
